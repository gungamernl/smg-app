<Window x:Class="SMG_app.login"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SMG_app"
        mc:Ignorable="d"
        Title="login" Height="450" Width="800">
    <Grid>
        <Grid Margin="20,20">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Width="200">
                <TextBlock Margin="0,10">gebruikersnaam</TextBlock>
                <TextBox Margin="0,10" x:Name="tb_adminname"></TextBox>
                <TextBlock Margin="0,10">wachtwoord</TextBlock>
                <PasswordBox Margin="0,10" x:Name="tb_adminpassword"></PasswordBox>
                <Button Margin="0,10" Click="login_Click">login</Button>

            </StackPanel>
        </Grid>
    </Grid>
</Window>
